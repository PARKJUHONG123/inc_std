# db source url
spring.datasource.url=jdbc:mysql://localhost:3306/inc_std?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true

# db response name
spring.datasource.username=root

# db response password
spring.datasource.password=root

# 객체가 실제 sql 문으로 어떻게 저장되는지 출력
spring.jpa.show-sql=true